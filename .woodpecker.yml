variables:
  - &docker_creds
    username:
      from_secret: docker_username
    password:
      from_secret: docker_password

  - &deploy_creds
    image: bitnami/kubectl
    environment:
      KUBECONFIG_BASE64:
        from_secret: kubeconfig

steps:
  build:
    image: woodpeckerci/plugin-docker-buildx
    settings:
      repo: edrodefeld/luna
      auto_tag: true
      <<: *docker_creds
    when:
      - event: [push, manual]
        branch: main

  deploy:
    <<: *deploy_creds
    commands:
      - echo $KUBECONFIG_BASE64 | base64 -d > /tmp/kubeconfig
      - kubectl --kubeconfig=/tmp/kubeconfig rollout restart deployment/luna -n galaxy
    when:
      - event: [push, manual]
        branch: main
